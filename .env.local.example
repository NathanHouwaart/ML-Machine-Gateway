# Local-only configuration. Copy to .env.local; it is ignored by Git.
GATEWAY_BIND=127.0.0.1:8098
# A Windows folder shared with Docker Desktop. The portal receives it read-only.
TBDAQ_LOCAL_DATA_ROOT=C:\ML-Machine-Gateway-TestData
